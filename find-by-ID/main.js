/* eslint-disable no-unused-vars */

function findByID(id, collection) {
  for (i = 0, i < collection.length, i++) {
    /* set variable for id selector and child text */
    var $id = document.querySelector()
    /* argument to determine if id is === to current selected item as indicated by i */
    if (/* argument above is true then return the entire object */)

  }
}
